{
  "name": "REPLACE_ME_YOUR_APP_NAME-shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for REPLACE_ME_YOUR_APP_NAME",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.9.0",
    "rimraf": "^5.0.1"
  }
} 